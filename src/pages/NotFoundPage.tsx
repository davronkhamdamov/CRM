const NotFoundPage = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>Not found</h1>
    </div>
  );
};
export default NotFoundPage;
